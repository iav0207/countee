apply plugin: 'java'
apply plugin: 'idea'

repositories {
    mavenCentral()
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    testCompile 'org.testng:testng:6.9.10'
    compile project(':utils')
    compile 'org.mockito:mockito-core:2.0.87-beta'
}