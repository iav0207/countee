apply plugin: 'java'
apply plugin: 'idea'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])

    compile project(':vo')
    compile project(':json')
    compile project(':persistence')
}